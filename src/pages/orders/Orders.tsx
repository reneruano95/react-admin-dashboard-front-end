import { Typography } from "@mui/material";
import "./orders.scss";

const Orders = () => {
  return (
    <div>
      <Typography variant="h4">ORDERS</Typography>
    </div>
  );
};

export default Orders;
